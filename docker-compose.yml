version: "3.9"
services:
 useritem-api-service:
  image: brentw53/apidev-project
  ports:
    - "8000:8000"
  volumes:
    - sqlite_useritems_volume:/code/sqlitedb

volumes:
  sqlite_useritems_volume:
